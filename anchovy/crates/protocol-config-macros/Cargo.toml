[package]
name = "protocol-config-macros"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

# A copy of sui's `sui-protocol-config-macros`, without the RPC `render`
# method.

[lib]
proc-macro = true

[dependencies]
proc-macro2.workspace = true
quote.workspace = true
syn.workspace = true
